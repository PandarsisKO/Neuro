# R7 novelty-residual reading evidence — 2026-09-12

Commits `ae05963` and `bf8d595` release 0.63.43. The latter reuses one embedding snapshot for all Fast-wave
candidates. R7 uses stored normalized chunk embeddings as a project-relative comparison
surface. It compares every candidate to project members and the submitted cohort, excluding the candidate itself;
the result does not depend on enqueue order.

`novelty.profile` returns nearest-neighbor similarity per chunk and a source redundancy mean. The scheduler applies
only a bounded negative priority adjustment. It never filters a source: R6 still creates a durable warm job for each
non-fast source. Findings orders kept windows by their least-similar matching chunk, but uses the original indexes in
work-unit keys and sorts completed results back into source order before materialization. This preserves R4 recovery
and all existing completeness behavior.

The R7 gate covers a repeated source containing a critical novel exception, no-vector fail-open behavior,
batch-order independence, and retained warm eligibility. Final focused R7/R6/R4/R5 suite: **30 passed in 29.19 seconds**.

Final commit-bound release-check: **PASS in 213.0 seconds**. It includes full pytest, Tier 1, migration/recovery,
retrieval, browser, UI, storage and backup/restore gates. Artifact:
`evals/release/release-check-0.63.43-bf8d595-20260912-032952.json`. The initial implementation artifact at
`ae05963` is retained as intermediate evidence.

Limits: R7 uses the existing embedding corpus only. It does not claim a live latency reduction or semantic quality
improvement beyond deterministic scheduling/order behavior; those belong in the formal Foundation closeout report.
