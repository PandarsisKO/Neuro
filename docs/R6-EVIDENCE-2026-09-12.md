# R6 Fast/Warm evidence — 2026-09-12

Status: **COMPLETE**. Candidate `0.63.42`, commit `3fd0a2c`.

## Contract

For an interactive bulk request of six or more sources, R6 selects at most three fast sources without a model call.
The deterministic portfolio combines project priority, existing value, review relevance, open-target lexical fit,
creator and source-type diversity. Fast work is explicit `priority` / `api_requested`; every remaining source stays
as a durable `normal` / `local_preferred` warm job.

Fast results carry analysis provenance and remain visibly provisional in Sources. A later warm or deep analysis
replaces that marker. Promoting warm work modifies the existing job’s lane and execution policy; it does not create a
replacement job or discard R4 work units.

## Gate evidence

`tests/test_s48_r6_fast_warm.py` proves deterministic diverse selection, full warm eligibility, fast/warm policy
separation, same-job promotion, endpoint disclosure, and later replacement of provisional output.

Focused R6/R4/R5 tests: **26 passed**. Full pytest: **1,313 passed, one existing Starlette `BlockingPortal`
deprecation warning in 147.38 seconds**. Tier 1 passed with frozen totals. Commit-bound release-check passed in 5.9
seconds: `evals/release/release-check-0.63.42-3fd0a2c-20260912-030848.json`.

R6 is closed. R7 novelty-residual reading is admitted.
