# Neuro Search — notes for Claude Code

Python 3.11+ / FastAPI / SQLite (FTS5 + numpy vectors). Package lives in `neurosearch/`.

- `config.py` settings from env/.env · `db.py` schema + all SQL · `media.py` yt-dlp (enumerate, captions, audio) · `documents.py` PDF/DOCX/text → pages
- `transcribe.py` Whisper API · `chunking.py` segments→chunks · `embeddings.py` OpenAI embeddings
- `ingest.py` orchestration · `jobs.py` background workers · `search.py` hybrid retrieval · `qa.py` Claude answers w/ citations + project tools · `export.py` findings & masterplan package
- `api.py` REST + web UI + MCP mount · `mcp_server.py` MCP tools · `cli.py` typer CLI · `web/` single-page UI

Run locally: `pip install -e . && cp .env.example .env && neurosearch serve`. Tests: `pytest`.
YouTube may block datacenter IPs — ingestion can be run from a laptop with the same CLI against the same DB
(`NEUROSEARCH_DATA_DIR`) or with `NEUROSEARCH_COOKIES_FILE`.
Keep the DB schema additive (CREATE IF NOT EXISTS) — there is no migration tool.

UI model: projects are the unit; inside a project: chats (many, isolated to project sources), sources, findings, settings.
Documents use page numbers as start/end in segments/chunks; `fmt_locator` renders 'p. N' vs mm:ss.
Master Planner lives in `planner.py`: two streamed passes (ANALYSIS_SYSTEM → plan.analysis with SWOT/readiness/options/assumptions/failure_patterns; then SYSTEM → plan), material capped (MATERIAL_CHARS), `_repair_json` for cut-off output, runs as `build_plan` job (API `background: true`); `plan_markdown`/`plan_html`; statuses in plan_items keyed like `first_steps.0`.
Tests use `tests/fake_claude.py` in place of the Anthropic client.
Suggested findings: `findings.py`; notes have status suggested|approved|dismissed (list_project_notes defaults to approved).
Project steering fields (goal, audience, output_pref, source_prefs, questions, context) are rendered by `db.project_steering` into every prompt.
Discover sources: `discover.py` — pass 1 no-tools shortlist (instant, saved immediately), pass 2 web_search verify/top-up (`db.update_discovery`); `discoveries` table; UI pane in Sources polls during the job.
Course import: `extension/` (MV3; scanner.js runs in the page, popup.js posts to /api/projects/{id}/course-import) → `courses.py` writes data/cookies/<collection>.txt; ingest_url jobs carry cookies_file/referer/title/collection_id.
Spend/budget: `usage.py` (usage table, `guard()` raises BudgetPaused → jobs re-queue with not_before; kv table holds budgets + queue_paused).
Relevance ranking: `relevance.py` scores proposed channel/playlist videos (titles+descriptions, `rank_proposed` job) → sources.relevance/relevance_why; review meta in kv `review:<collection>` carries max_videos/ranked; the UI pre-ticks the best `max_videos`.
Web pages: `webpage.py` (fetch + section HTML; PDF links go through documents.extract_pages) → platform 'web', sections cited as '§ N'; `media.classify_url` returns 'web' for non-media hosts. API endpoints are plain `def` (threadpool) so SQLite work never blocks the event loop; `db.batch()` groups bulk-listing writes.
Sites that block fetches (403) raise `webpage.Blocked` with a hint; the extension's 'Send this page' posts the rendered HTML to /api/projects/{id}/ingest/html.
Instagram: profiles only with the user's own session via the extension (`media.enumerate_instagram`, cap IG_MAX=40, review card, per-site politeness in `media.polite`); review meta carries cookies_file/referer into ingest_source jobs.
Retries: transient errors (jobs.TRANSIENT) re-queue with backoff up to MAX_ATTEMPTS (payload._attempts); failed jobs can be retried (/api/jobs/{id}/retry, /api/jobs/retry-failed) or dismissed.
NEVER write to data/neurosearch.db from outside the running app (another OS/VM through a mount corrupts the WAL and loses recent transactions) — use the API/CLI. Hourly snapshots: db.backup() → data/backups/.
Spreadsheets: `sheets.py` — xlsx/csv → platform 'spreadsheet' (sheets as pages, locator 'sheet N'), file kept in data/files/, labelled inputs/outputs in kv `sheet:<source_id>`; `formulas` package recomputes the workbook (calculate(source_id, inputs, outputs)); chat gets a `calculate` tool when the project has spreadsheets; UI: 🧮 Calculator dialog, /api/sources/{id}/calculator + /calculate.
YouTube search links (youtube.com/results?search_query=…) → `media.enumerate_search` (ytsearchN) → review card like a playlist; Discover renders a 🔍 Search YouTube button for them.
Prompt caching: `usage.cached_block`/`usage.mark_last` put cache breakpoints on the stable prefixes — chat rules+project block (+ full-context excerpts when ≤6 sources) and the conversation tail (tool rounds/turns), findings/relevance project head, planner research material (first system block, shared by both passes). `usage.record` prices cache_read (0.1x) / cache_write (1.25x) and stores `saved`; shown in Settings and the fun stats card. Prefixes under ~1024 tokens are not marked (the API ignores them).
Hardening (see HARDENING.md): `providers.py` is the only place clients are made (`NEUROSEARCH_FAKE_AI=1` → `fake_ai.py`, content-aware fakes; every call declares its task in extra_headers x-neurosearch-task — the router hook). `evals.py` + `neurosearch eval` run the frozen Golden Project (tests/fixtures/golden) end to end with Tier-1 gates; `evidence.py` validators run at runtime (finding quotes must be in the transcript window, answer citations must exist, plan evidence ids must resolve) and count into kv `evidence:*` → `/api/health`. `db.backup()` verifies every snapshot (`verify_database`), `db.integrity_check()` hourly; migration fixtures in tests/fixtures/db (built by build.py from old db.py copies). `media.canonical_url` before any dedupe. `logctx.py` correlates log lines (job/run/project/source/task/model).
Data trust (0.16): project-relative analysis lives in `project_source_analysis` keyed (project, source, analysis_kind ∈ relevance|summary) — one artifact per AI task with its own provenance + `input_hash` (`findings.input_hash`, `relevance.input_hash`; `db.upsert_analysis(pid, sid, kind, …)`, `project_analysis(pid, kind)`, `project_analyses`, `get_analysis(pid, sid, kind)`; `set_source_summary`/`set_relevance` require project_id); rows migrated from 0.15 carry status `legacy_unverified` and are never current — never write summary/substance/relevance on `sources`. Revisions: `db.segments_revision` → sources.revision; `db.brief_revision/facts_revision/source_set_revision/project_revisions`; notes/plans/discoveries carry them + model/prompt_version. `staleness.assess/rebuild` (+ /api/projects/{id}/staleness, /rebuild-stale): informational, cost-quoted, jobs go through usage.guard; REBUILDING/failed/cancelled derive from job rows; the plan job is created with `blocked_by` = the findings jobs (`db.create_job(..., blocked_by)`; `claim_job` skips blocked jobs and fails them when an upstream job failed); chats never stale. `db.delete_source` marks citations/evidence `removed` (planner emap entries carry source_id).
Indestructible work (0.17): `jobs.py` = durable SQLite queue — `db.claim_job(kinds, worker_id)` takes a lease (run_id/lease_until), `db.heartbeat`, `db.recover_expired_leases` (also at start), `db.finish_job` guarded by run_id, `db.derived_status` (blocked/retry_wait/budget_wait/rate_limit_wait/cancelling), `db.job_event`/`job_events` history, `db.create_job(..., blocked_by, dependency_policy, dedupe_key)` (dedupe_key_for), `db.request_cancel` + `jobs.check_cancel()` at safe boundaries, `jobs.submit_external` → `external_pending` (never resubmit; `jobs.EXTERNAL` handlers, FakeExternal), `jobs.execute(job)` is the one place a job's outcome is decided. Ingestion is staged (`sources.stage`, `db.set_stage` inside the same batch as the stage's data; audio_path reused; embeddings granular) — `ingest_source` resumes at the first incomplete stage. `jobs.crash_point(name)` + tests/crashkit.py drive the crash matrix and the 40-source equivalence gate; `fixture://path.json` is a deterministic platform for that.
Invocation ledger (0.17.1): `providers.py` wraps every client method (`_Ledgered`/`_LedgeredStream`) — `db.invocation_start` (intent→in_flight) before the network call, `invocation_finish` after; `recover_expired_leases` marks a dead run's in-flight rows `outcome_unknown` (`ambiguous_external_execution` event, Health `invocations.ambiguous`). Cancel keeps completed stages; `db.DependencyCycle` guards blocked_by (`db.set_dependencies`); `jobs.CANCEL_AT` test hook.
Transport retries (0.17.2): SDK max_retries=0; `providers._Ledgered` retries typed transient errors (`classify_error`, `RETRY_POLICY` per task) with one invocations row per attempt (logical_id/attempt_no/error_type); failures raise `providers.ProviderError(error_type)`.
Model router (0.18): `contracts.py` declares an InferenceContract per task (configured model, thinking/effort, max_output_tokens, timeout, transport attempts, execution class); `providers.invoke(task, system=, messages=, tools=, stream=)` is the only entry point for model calls — never pass model/max_tokens/extra_headers at call sites. Claude 4 requests carry nothing extra; Claude 5 gets `thinking`/`output_config.effort`. Per-task overrides: NEUROSEARCH_TASK_MODEL_<TASK> etc.; `neurosearch contracts`; eval `--task-model/--task-thinking/--task-max-tokens`. Router-equivalence gate = test_router_equivalence_fake_tier1 (frozen fake numbers).
Batch findings (0.20.0, Rung G1): `batches.py` — job kind `suggest_findings_batch` (`db.create_job(..., {"project_id", "source_ids"?, "force"?})`) plans logical items via `findings.batch_requests` (stable custom_id per window, frozen params with 1h cache ttl), records them in `batch_items` (cohort_no, status planned→submitted→succeeded/errored/expired/canceled→materialized, raw result kept locally), submits ONE provider batch per cohort through `jobs.submit_external("anthropic_batch", ..., client_ref="<job>#<cohort>")` (`AnthropicBatch` handler: submit/find_by_ref(_attach)/check/persist_results/cancel), parks, and on results materialises every source whose windows all succeeded with `findings.materialize(..., transport="batch", batch_id=)` — the same function the interactive path uses; failed items go to the next cohort (same custom_ids) up to `MAX_COHORTS`. `batches.estimate(pid)` → now/background/discount/note for the UI; `usage.record(transport="batch")` prices model tokens ×`BATCH_MULT`; `batches.cancel_job` harvests completed results after a cancel. Fakes: `fake_batches.FakeBatches` (durable, knobs NEUROSEARCH_FAKE_BATCH_{READY_AFTER,FAIL_ONCE,EXPIRE_ONCE,KEEP_ON_CANCEL}). Never write batch results into the DB except through `batches.materialize_ready`. Recovery of an unknown handle on the real API (0.20.0+g2): candidates (created after intent, same count, not rejected) → job parks on `tentative:<ref>` (derived `external_tentative` / `external_handle_ambiguous`), `_check_tentative` verifies the returned custom_id SET before `_attach`; rejected ids in kv `batch:rejected:<ref>`; none left → `db.unpark_external` → resubmit. Never cancel/mutate a candidate. Transport is explicit per request: `POST /suggest {transport}`, `POST /suggest/estimate` (`batches.estimate`: count_tokens cached in kv `tokcount:<custom_id>`, `BULK_THRESHOLD_ITEMS` recommendation, `CHOICES` wording), `POST /rebuild-stale {transport}` (`staleness.rebuild(transport="batch")` = one batch job + plan blocked on it); `batches.ui_state(job)` is what the UI shows (`job.batch`). Auto-analysis after ingest stays interactive. `neurosearch batch-smoke [--live]` (`batch_smoke.py`): the one tiny real-provider check of the adapter (eval-only temp DB, golden yt01 via `evals.load_golden(only=)`, one batch item, all invariants, report under evals/batch-smoke/); never touches production data.
Cache layout (0.20.0+g4): `qa.chat_system_blocks(project, use_web, tools, full_context)` builds the chat system prompt in cache order — stable [rules+web rule+project identity/brief/tool guidance/steering → breakpoint] [full-context material → breakpoint] then `PROJECT_STATE_BLOCK` (pinned findings + facts) unmarked after the prefix; tools are built BEFORE the system blocks and their JSON size counts toward the ≥1024-token minimum (`cached_block(min_chars=)`). `findings._system_blocks` = [rules][project framing→bp][source framing→bp] (`HEAD_SEP`). `qa._tail_breakpoint` wraps `usage.mark_last` — OFF by default (env `NEUROSEARCH_CHAT_TAIL_BREAKPOINT=1` enables; no adaptive logic by decision). Rung G complete at 0.20.0+g5. `neurosearch eval --cache-layout` (`cache_layout.py`) measures read/write/plain per scenario under `fake_ai._simulate_cache` (provider-faithful; `fake_ai.CACHE_LOG`); `cache_layout.request_content` is the line-multiset content signature used by the equivalence test. Frozen Tier 1 numbers are TOTAL input (plain+read+write) — never change prompt content without re-running the equivalence test.
Findings pre-filter (0.21.0+h1, Rung H1, OFF by default — NEUROSEARCH_FINDINGS_PREFILTER=1): `prefilter.py` — contract `findings.prefilter` (`contracts.PREFILTER_MODEL`, schema `prefilter-v1`), `decide()` never raises (fail-open → `uncertain` with `fail_open`), `decide_windows()` adds the aggressive-source signal, decisions durable in `window_decisions` keyed by `prefilter.input_hash` (window text + brief revision + prompt/schema/model + sample size); `findings.window_plan(project, src, windows)` is the single routing point for interactive AND batch (`batch_requests` omits dropped windows, keeps original indexes; `batches.materialize_ready` iterates sorted indexes); `materialize(..., prefilter=)` stores the summary in `project_source_analysis.prefilter`. `prefilter.SAMPLE_CHARS` (env NEUROSEARCH_PREFILTER_SAMPLE_CHARS, default 0 = whole window). Eval: `neurosearch eval --prefilter` (`prefilter_eval.py`, fixture tests/fixtures/golden/prefilter/ + labels.json; build.py regenerates). Fake: `fake_ai._prefilter` lexical; knobs NEUROSEARCH_FAKE_PREFILTER=drop_all|bad_json|refuse|error. Never let the pre-filter extract findings. H1 status (0.21.0+h2): DEFERRED — at current prices (`usage.PRICES` Haiku $1/$5) the filter loses money against Sonnet 5 batching (`prefilter_eval.economics` reports interactive / background / background-batched-filter scenarios with break-even irrelevant share; gate = background ≥10% net, ≥1.25× leverage); keep it off, do not enable or run live without re-measuring.
Retrieval eval (0.22.0+i1, Rung I1): `neurosearch eval --retrieval [--baseline]` (`retrieval_eval.py`; fixture tests/fixtures/golden/retrieval/ — build.py regenerates retrieval.json + 4 distractor transcripts) runs production `search.search` unchanged over 30 hard queries graded per source + chunk locator with hard negatives; reports R@1/3/5/10, MRR, NDCG@10, ranks, locator/exact-locator, hard-negative FPs, per-query top-40 candidates (`run(search_fn=)` lets an experiment plug a reranked search; `compare()` tracks rank changes). Baseline frozen in evals/retrieval/ and `test_retrieval_baseline_is_frozen`. No reranker exists; if one is built it may only reorder the fused candidates and must fall back to today's ordering. `--retrieval --live` needs only OPENAI_API_KEY (no Anthropic calls); `retrieval_eval.headroom()` applies the frozen I1.5 gate (`GATE`) and computes the minimum rerank depth from candidate positions.
Reranker (0.22.0+i2, Rung I2, OFF — NEUROSEARCH_RETRIEVAL_RERANK=1): `rerank.py` — contract `retrieval.rerank` (`contracts.RERANK_MODEL`, schema `retrieval-rerank-v1` = permutation of candidate numbers), `RERANK_DEPTH` 11, `rerank(query, hits)` reorders only the first depth hits and fails CLOSED to the input ordering on any failure (`validate_order`; `rerank_fallback` event + kv; Health `rerank`); `search.search(rerank=)` (None = settings flag); hits carry `rerank={from,to,version,model}`; usage kind `rerank`. `neurosearch eval --retrieval --rerank [--live]` → `retrieval_eval.run_rerank_compare` with the FROZEN adoption rule (`LIVE_BASELINE`, `HARD_GATES`, `MEANINGFUL`, `FIXABLE_IMPROVED_MIN`) → ADOPT/KILL. Fake `fake_ai._rerank` + NEUROSEARCH_FAKE_RERANK knobs prove mechanics only. Never let the reranker add or drop candidates; never tune it repeatedly to the fixture. Rung I closed (0.22.0+i3): the live comparison was a KILL (MRR 0.9093 → 0.8903, R@1 86.7% → 83.3%, +1.67 s/query) — production retrieval stays FTS + embeddings + RRF; do not re-enable or retune the reranker without a new decision.

Ranking eval (0.18.0-e2.0): `neurosearch eval --ranking` runs the frozen rank.relevance fixture (tests/fixtures/golden/ranking.json, 79 graded candidates; `build_ranking.py` regenerates it and a test asserts it is unchanged) through `evals.run_ranking` → `relevance.rank_collection` → `providers.invoke("rank.relevance")`; reports P/R@k, NDCG@20, per-category ranks, schema validity (`rank_collection` returns batches/failed_batches/repaired_batches), unscored, tokens, cost per 100 candidates, latency, configured/returned model; ranking baselines are `evals/baseline-rank-*.json` (`compare_ranking`). The fake ranker is lexical (project-head word overlap), never grade-aware.
Ranking model comparison (0.18.0-e2.1): `neurosearch eval --ranking-compare [--live] [--baseline-model m] [--candidate-model m]` → `evals.run_ranking_compare` runs `run_ranking` twice under `NEUROSEARCH_TASK_MODEL/THINKING_RANK_RELEVANCE` overrides (restored afterwards), `relevance.canonical_requests` + `messages.count_tokens` give canonical input tokens/tokenizer delta (fake: Claude 5 family ×1.3), `_side_by_side` + `ranking_verdict` (PASS / PASS_WITH_CAVEAT / FAIL; quality+validity gate, cost/latency caveats) → `evals/ranking-compare/<stamp>-<sha>/`. Never changes a production default.
E2.1 (0.18.0-e2.2): rank.relevance runs on `contracts.RANK_MODEL` = claude-sonnet-5 with thinking explicitly disabled (all other tasks: settings.answer_model). Findings comparison: `neurosearch eval --findings-compare [--live]` → `evals.run_findings_compare` (load_golden once, `run_findings` per model with force=True under NEUROSEARCH_TASK_*_FINDINGS_EXTRACT overrides, `findings.canonical_requests` + count_tokens, `findings_verdict`; hard gates = evidence recall/quote integrity/incomplete outputs, findings count never) → `evals/findings-compare/<stamp>-<sha>/`. `findings.OBSERVER(event)` gets one dict per window (stop_reason/truncated/parse strict|repaired|failed|no_json/raw/kept/rejected/summary_ok/substance_ok); `_call` behaviour is unchanged.
E2.2 (0.18.0-e2.3): findings.extract runs on `contracts.FINDINGS_MODEL` = claude-sonnet-5, thinking explicitly disabled. Migrated so far: rank.relevance, findings.extract. All other tasks: settings.answer_model (claude-sonnet-4-6), plain request shape.
Migration comparison (0.18.0-e2.4): `neurosearch eval --migration-compare [--live]` → `migration.run_migration_compare`: shared inputs once (load_golden + findings + approve), arms via NEUROSEARCH_TASK_* overrides (`_set_arm`/`_restore_env`), `_restore_state(pid, mark)` between arms, per-task runners (`run_planner_arm` analysis+build from one build_plan, `run_update_arm`, `run_export_arm`, `run_chat_arm`), frozen `planner_rubric.json` scored by `score_rubric`/`grounding` (deterministic regex checks per section; no model judges), verdicts `planner_verdict` (adaptive only wins on measurable rubric/failure-mode/completeness gain), `update_verdict`, `export_verdict`, `chat_verdict` (→ chat + repair). Observer hooks: `qa.OBSERVER`, `planner.OBSERVER`, `export._last_call`. discover.* excluded.
Mission E closed (0.18.0): rank.relevance + findings.extract on Sonnet 5 (thinking disabled); every other Anthropic task stays on settings.answer_model (claude-sonnet-4-6) by decision — deferred, not failed. Eval policy: Tier 1 + pytest always; live paid evals only for major model replacement, major AI-affecting architecture change, or regression investigation — never a routine manual step.
Mission F (0.19.0-f1): `schemas.py` registry (findings-v2, rank-v2; `provider_schema` strips client-only constraints, `validate` uses jsonschema on the full schema, `check_provider_compat` runs from `contracts.validate`); contract `.schema` → `request_params` adds `output_config.format`; `NEUROSEARCH_TASK_SCHEMA_<TASK>=none` disables. `providers.structured(task, resp)` = strict parse + validate → raises `OutputError(TRUNCATED|REFUSED)` / `SchemaMismatch`; call sites (findings._call, relevance._call) escalate max_tokens once on truncation, and on mismatch use the legacy parser as an OBSERVABLE fallback (`providers.schema_fallback` → validation_events + kv evidence:schema_fallbacks → health.structured_outputs; steady state 0; Tier 1 gates it). Schema version is in `findings.input_hash`/`relevance.input_hash` and provenance. Fake validates its own structured output; `NEUROSEARCH_FAKE_AI_BAD_JSON=1` exercises the fallback.
F1/F2 corrections + F3 (0.19.0-f3): `providers.invoke_structured(task, system=, messages=, usage_kind=, project_id=, source_id=, guard_estimate=, legacy=)` is THE structured path (json.loads → full jsonschema validation; mismatch = bug signal → one fresh retry → typed SchemaMismatch; truncation → one guarded escalation to min(1.5×, contract.max_output_ceiling); refusal typed; `providers.last_response()` for provenance). Legacy parsers run only for schema-less contracts or under `NEUROSEARCH_SCHEMA_COMPAT_FALLBACK=1` (result must still validate). `schemas.check_installation()` at startup — jsonschema is required. Structured tasks: findings.extract, rank.relevance, planner.update (plan-update-v2; failures propagate, never []), discover.quick (discovery-v2); discover.verify stays free-text (citations ⟂ output_config.format).
F4 (0.19.0-f4): `planner_v3.py` behind `NEUROSEARCH_PLANNER_V3=1` (`settings.planner_v3`; `planner.build_plan` dispatches). Tasks planner.situation/core/execution/economics/actions (schemas situation-v3, plan-*-v3), shared cached material block, frozen analysis (`_analysis_hash`), `assemble()` = deterministic Python + cross-component checks (`PlanAssemblyError` → previous plan kept, `plan_assembly_failed` event), semantic ids (`norm_id`, `_ids` positional→id, `reconcile_ids` on rebuild; `db.save_plan` carries statuses by id when both plans have `_ids`), `_build` telemetry (cache read/write per component), `_components`. V1 planner = rollback for one release. Fakes: `fake_ai.PLANNER_V3` canned components.
F5 (0.19.0-f5): `neurosearch closeout [--live] [--no-pytest]` → `closeout.run_closeout`: `deterministic_phase` (pytest in a clean env, registry compat, Tier 1 V1 + V3, rubric) then `live_phase` (findings, ranking vs saved Sonnet 5 envelope, planner.update, discover.quick pass 1 via `discover(..., verify=False)`, Planner V1 vs V3 via migration.run_planner_arm) → `planner_decision` (PROMOTE / DO NOT PROMOTE) + Mission verdict → evals/mission-f-closeout/<stamp>-<sha>/. Fake plan updates are content-aware (`fake_ai._updates`). Latency caveats only when the baseline is ≥ 1 s.
F5 harness (0.19.0+f6): `planner.research_context(pid, strict=)` freezes the planner input; `build_plan(..., research=)` / `build_plan_v3(..., research=)` consume it (plan `_research_hash`); `search.search(strict=True)` raises `RetrievalUnavailable`, non-strict bumps `evidence:retrieval_degraded`. `neurosearch closeout --resume`: run dir keeps `data/` (its DB) + `stages.json`; `_import_previous` reuses done stages (planner arms only on matching research_hash), records `history`; dependency failures → stage INCOMPLETE → verdict INCOMPLETE / NOT DECIDED.
Mission F closed (0.19.0): structured outputs live on findings.extract, rank.relevance, planner.update, discover.quick. Planner V3 NOT promoted (live: `planner.situation.options[].fit` came back 90/45/75 then 9/6 against a local 1..5 constraint that the provider schema cannot carry → SchemaMismatch → typed failure); V1 is the production planner, V3 stays experimental behind NEUROSEARCH_PLANNER_V3; no tuning loop. If V3 is revisited: fix `fit` scale semantics first, then audit every stripped numeric constraint. Known minor cleanup: planner_decision's "truncation / parse failure / JSON repair" line should name the actual failure kind.
