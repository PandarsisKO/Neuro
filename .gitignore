.env
# any local copy/backup of .env holds live secrets (tunnel key, IdP config) — never commit one
.env.*
!.env.example
*.env.bak*
data/
*.db
*.db-wal
*.db-shm
.venv/
__pycache__/
*.pyc
*.egg-info/
build/
dist/
.DS_Store
cookies.txt
data/backups/
_fixture_src/
_update.tgz
_to_delete/
VIDEOS/
data_backup_*/
!tests/fixtures/db/*.db

# design audit instance (tools/audit-instance.command) — private copy of a backup, never tracked
data-audit/
.worktrees/

# scanner gate deps (npm ci --prefix tests/js)
tests/js/node_modules/

# Kyle's reference material and session scratch at the repo root — never tracked (2026-09-21 audit)
INSPIRATION/
SCREENSHOT AUDIT/
Claude outputs/
.*_livetest/

# Mac-native git publisher runtime queue (requests/receipts; never tracked)
.git-publisher/
